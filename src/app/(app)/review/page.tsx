import { RoutePlaceholder } from "@/components/dev/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      route="/review"
      purpose="The SRS review session. Answers what should I review today, independently of the curriculum."
    />
  );
}
