import { RoutePlaceholder } from "@/components/dev/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      route="/explore"
      purpose="Immersion and free exploration: content browsed by interest rather than by curriculum order."
    />
  );
}
