import { RoutePlaceholder } from "@/components/dev/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      route="/home"
      purpose="The daily surface: what is due for review, and the next lesson in the curriculum."
    />
  );
}
