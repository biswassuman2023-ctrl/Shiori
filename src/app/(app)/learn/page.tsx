import { RoutePlaceholder } from "@/components/dev/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      route="/learn"
      purpose="The curriculum map: all seven levels and the learner's position on the ladder."
    />
  );
}
