import { RoutePlaceholder } from "@/components/dev/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder route="/profile" purpose="Account, skill mastery, streaks and settings." />
  );
}
