import { RoutePlaceholder } from "@/components/dev/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      route="/"
      purpose="Marketing home. States the promise: learn Japanese from where you actually are, and routes visitors into onboarding."
    />
  );
}
