import { RoutePlaceholder } from "@/components/dev/route-placeholder";

export default function Page() {
  return <RoutePlaceholder route="/auth" purpose="Sign in and sign up, backed by Supabase Auth." />;
}
