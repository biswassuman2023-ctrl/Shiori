import { RoutePlaceholder } from "@/components/dev/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      route="/onboarding"
      purpose="Collects goals, prior exposure and pace. Its answers decide whether the learner takes a diagnostic or starts at Hiragana."
    />
  );
}
