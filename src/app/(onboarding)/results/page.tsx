import { RoutePlaceholder } from "@/components/dev/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      route="/results"
      purpose="Explains what the diagnostic found, per skill, before proposing a placement."
    />
  );
}
