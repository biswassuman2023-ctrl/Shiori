import { RoutePlaceholder } from "@/components/dev/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      route="/placement"
      purpose="Proposes a starting point on the fixed ladder and lets the learner accept it or start earlier."
    />
  );
}
