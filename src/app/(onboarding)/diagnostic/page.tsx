import { RoutePlaceholder } from "@/components/dev/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      route="/diagnostic"
      purpose="Adaptive diagnostic. Estimates per-skill ability so placement has evidence rather than a self-report."
    />
  );
}
